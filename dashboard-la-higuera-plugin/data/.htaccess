# Permitir acceso a archivos CSV
<FilesMatch "\.(csv)$">
    Order Allow,Deny
    Allow from all
    Satisfy Any
</FilesMatch>

# Prevenir ejecución de PHP
<FilesMatch "\.php$">
    Order Deny,Allow
    Deny from all
</FilesMatch>
